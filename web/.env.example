# URL pública del backend (Lambda Function URL). Copia este archivo a .env y rellénalo.
# Se obtiene del output `function_url` de Terraform.
VITE_API_URL=https://XXXXXXXXXXXX.lambda-url.us-east-2.on.aws
